# Makes a square with Turtle 

# Challenge #6

import turtle

window = turtle.Turtle() # Opens the window

shub = turtle.Turtle() # Name

# Moves the turtle

shub.forward(200)
shub.left(90)
shub.forward(200)
shub.left(90)
shub.forward(200)
shub.left(90)
shub.forward(200)

# Keeps the window open

turtle.getscreen()._root.mainloop()
